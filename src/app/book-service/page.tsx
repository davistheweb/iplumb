import { BookService } from "@/components/book-service/BookService";

export default function BookServicePage() {
  return (
    <div className="h-full w-full overflow-x-hidden">
      <BookService />
    </div>
  );
}
